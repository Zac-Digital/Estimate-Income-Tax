name: SonarQube

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/**'
      - '!src/IncomeTax.Presentation.Web.Node/**'
      - 'test/**'
      - '!test/IncomeTax.Presentation.Web.Functional/**'
      - '.github/workflows/sonarqube.yml'
      - '.github/workflows/unit-testing.yml'
  push:
    branches:
      - 'main'
    paths:
      - 'src/**'
      - '!src/IncomeTax.Presentation.Web.Node/**'
      - 'test/**'
      - '!test/IncomeTax.Presentation.Web.Functional/**'
      - '.github/workflows/sonarqube.yml'
      - '.github/workflows/unit-testing.yml'

concurrency:
  group: '${{ github.workflow }}-${{ github.ref }}'
  cancel-in-progress: true

permissions: { }

jobs:
  build-test-analyse:
    if: ${{ github.actor != 'dependabot[bot]' }}
    name: Build, Test & Analyse
    runs-on: ubuntu-24.04
    permissions:
      contents: read       # For SonarQube
      pull-requests: write # For SonarQube
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup .NET
        uses: ./.github/actions/setup-dotnet

      - name: Install .NET Tooling
        shell: bash
        run: |
          dotnet tool install --global dotnet-sonarscanner
          dotnet tool install --global dotnet-coverage

      - name: Start Sonar Scanner
        shell: bash
        run: |
          dotnet-sonarscanner begin \
          /k:"zac-digital_estimate-income-tax" \
          /o:"zac-digital" \
          /d:sonar.token="${SONAR_TOKEN}" \
          /d:sonar.coverage.exclusions=**/src/IncomeTax.Presentation.Web/Pages/**,**/src/IncomeTax.Presentation.Web.Node/**,**/test/IncomeTax.Presentation.Web.Functional/** \
          /d:sonar.cpd.exclusions=**playwright.config.ts \
          /d:sonar.cs.vscoveragexml.reportsPaths=coverage.xml
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: .NET Restore
        shell: bash
        run: dotnet restore

      - name: .NET Build
        shell: bash
        run: dotnet build --no-restore --no-incremental --configuration Release

      - name: .NET Test
        shell: bash
        run: dotnet-coverage collect "dotnet test --no-build --no-restore --configuration Release" -f xml -o "coverage.xml"

      - name: Stop Sonar Scanner
        shell: bash
        run: |
          dotnet-sonarscanner end \
          /d:sonar.token="${SONAR_TOKEN}"
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}